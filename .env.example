# Google Cookie (ОБЯЗАТЕЛЬНО!)
# Получите cookie из https://labs.google/fx/tools/image-fx
GOOGLE_COOKIE=your_cookie_here

# API настройки (опционально)
# 0.0.0.0 = слушать на всех интерфейсах (нужно для Docker!)
API_HOST=0.0.0.0
API_PORT=8080  # Измените на свободный порт, если нужно

# Логирование
LOG_LEVEL=INFO
